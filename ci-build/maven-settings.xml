<settings 
	xmlns="http://maven.apache.org/SETTINGS/1.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 http://maven.apache.org/xsd/settings-1.0.0.xsd"
>
  <servers>
    
    <server>
			<id>knetminer-snapshots</id>
			<!-- This is to be set in your OS. In Travis, it's typically part of your project settings -->
      <username>${env.KNET_REPO_USER}</username>
      <password>${env.KNET_REPO_PASSWORD}</password>
	  </server>

    <server>
			<id>knetminer-releases</id>
      <username>${env.KNET_REPO_USER}</username>
      <password>${env.KNET_REPO_PASSWORD}</password>
	  </server>
    
  </servers>
  
</settings>
